//How the hell do I integrate other files?
//#include "main.h"

struct pixel {
 LocationType coord;
 bool set;
};

void openMenu();

task main()
{
SetSensorType(OUT_B, SENSOR_TYPE_ROTATION); //Port B, x-axis
SetSensorType(OUT_C, SENSOR_TYPE_ROTATION); //Port C, y-axis

ClearSensor(OUT_B); ClearSensor(OUT_C);

int x1, y1, x2, y2;
 while(true)
 {
 if(ButtonPressed(BTNCENTER,true)){
   openMenu();
   //TextOut(0, LCD_LINE2, "Hello!",DRAW_OPT_NORMAL);

 }
  x1=MotorTachoCount(OUT_B); // get line start
  y1=MotorTachoCount(OUT_C);
  x2=MotorTachoCount(OUT_B); //get line end
  y2=MotorTachoCount(OUT_C);
  //LineOut(x1/10, y1/-10,   x1/10, y1/-10);
  PointOut(x1/10, y1/-10);
 }
}

//if possible, this should be in a separate file
void openMenu()
{
  //save image data
  //create graphics
  //handle menu navigation
  //clear screen
  //restore image data
}

sub saveImage()
{
//write image data to array
}

sub restoreImage()
{
//load image data from array
}
